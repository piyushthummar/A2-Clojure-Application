To run this application, "sales.clj" file must be in the clojure folder along with "cust.txt", "prod.txt" and "sales.txt".
After that in command line, go until that path where "sales.clj" file is stored.
Then enter the following command to run the application.
1. java -cp clojure.jar clojure.main sales.clj